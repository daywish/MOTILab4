@model IEnumerable<MOTILab4.Models.Elector>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ElectorName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ElectorRatio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ElectorName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ElectorRatio)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ElectorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ElectorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ElectorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
